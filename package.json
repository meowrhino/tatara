{
  "name": "tatara",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute shop --remote --file=schema-tatara.sql"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.69.0"
  }
}
